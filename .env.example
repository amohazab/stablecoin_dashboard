# Archive access required - every Tier-1 read is pinned to run_block (P-3.05).
ETH_RPC_URL=

# Free tier. Used only by the spot-check sheet generator (P-3.13).
ETHERSCAN_API_KEY=

# RETIRED BY RULING (P-3.19, 2026-09-04). Dune's free plan becomes view-only on
# 2026-09-24 — query execution, including the API, moves behind a paid plan, which
# is not a realistic dependency for a check that fires only on a >25% supply jump.
# DET-62's second confirmation source is now the Etherscan token-supply endpoint,
# read with ETHERSCAN_API_KEY above. Kept as a comment for the historical record,
# not deleted; no new key is introduced.
# DUNE_API_KEY=
