Revision instructions (pass 2). The user message also carries `pass1_text`, the text this slot shows on the page, and `pass1_defects`, the evaluator's findings on it. Do not write a new text. Return `replacements`: for each defect you fix, its `quoted_span` copied exactly from `pass1_defects`, and the `replacement` text that takes its place in `pass1_text`. Nothing outside those spans changes: no other sentence is added, removed or reworded. Every replacement follows the rules and the slot's rules above, and the whole text must still meet the slot's paragraph count. Return in `references_field_ids` the field id of every row whose number or fact a replacement uses.
